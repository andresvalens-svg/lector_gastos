{
  "name": "lector-gastos-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npx tailwindcss -i src/input.css -o css/output.css",
    "dev": "npm run build && npx serve . -p 3000",
    "start": "npx serve . -p 3000"
  },
  "devDependencies": {
    "serve": "^14.2.0",
    "tailwindcss": "^3.4.0"
  }
}
